\documentclass[a4paper,11pt]{report}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[spanish,es-noshorthands]{babel}
\usepackage{lmodern}
\usepackage[a4paper]{geometry}
\geometry{top=2.5cm, bottom=2.5cm, left=2.2cm, right=2.2cm}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{enumitem}
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, calc}
\usepackage[hidelinks]{hyperref}

\renewcommand{\thesection}{\arabic{section}}
\setcounter{secnumdepth}{3}

% ---- Estilo de listados de codigo (Python) ----
\definecolor{codegray}{gray}{0.95}
\definecolor{codegreen}{rgb}{0.0,0.5,0.0}
\definecolor{codeblue}{rgb}{0.1,0.1,0.7}
\lstdefinestyle{py}{
  language=Python,
  backgroundcolor=\color{codegray},
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\color{codeblue}\bfseries,
  commentstyle=\color{codegreen}\itshape,
  stringstyle=\color{purple},
  numbers=left, numberstyle=\tiny\color{gray}, numbersep=6pt,
  showstringspaces=false, breaklines=true, frame=single, tabsize=4,
  captionpos=b
}
\lstset{style=py}

\title{\textbf{Control Adaptativo por Self-Tuning Regulator (STR) \\ del Péndulo Invertido en Tiempo Discreto} \\[0.4em]
\large Memoria Técnica}
\author{Lucas Gaggino \\ \small Facultad de Ingeniería - UBA \\ \small 86.20 Control Adaptativo}
\date{\today}

\begin{document}

\maketitle
\tableofcontents
\newpage

% ==================================================================
\section{Introducción}
% ==================================================================

Este trabajo implementa y valida un controlador adaptativo del tipo
\emph{Self-Tuning Regulator} (STR) para estabilizar el péndulo invertido en su
posición vertical. El objetivo del trabajo práctico es:

\begin{enumerate}[nosep]
  \item Implementar un controlador para la planta.
  \item Evaluar su comportamiento por colocación de polos.
  \item Implementar un mecanismo de autoajuste.
  \item Validar el funcionamiento ante distintas condiciones.
\end{enumerate}

En este proyecto el diseño del regulador se realiza enteramente en tiempo discreto. La
planta nominal se discretiza una única vez mediante ZOH analítico; a
partir de allí, la identificación y el diseño del regulador son íntegramente
discretos: la planta se describe por su modelo ARX y el controlador se obtiene
resolviendo la ecuación Diofantina en el operador retardo $q^{-1}$, \textbf{sin
transformar el controlador nuevamente a tiempo continuo} (p.\,ej.\ por Tustin),
siguiendo el método STR.

La estructura elegida es un \textbf{STR adaptativo indirecto}: en línea se
estiman los polinomios $A(q)$ y $B(q)$ de la planta por mínimos cuadrados
recursivos (RLS) y, por \emph{certainty equivalence}, se rediseña el regulador
resolviendo la ecuación Diofantina $A R + B S = A_{lc}$.

% ==================================================================
\section{Marco Teórico: el Self-Tuning Regulator}
% ==================================================================

\subsection{Control adaptativo y self-tuning}

Un controlador \emph{self-tuning} converge al que se hubiese hallado con las
técnicas habituales si la planta fuera conocida. Se supone conocida la
\emph{estructura} del modelo; los \emph{parámetros} son desconocidos y se
estiman on-line. Existen dos variantes:

\begin{itemize}[nosep]
  \item \textbf{Indirecto (explícito):} se estiman $A(q)$, $B(q)$ y luego se
  diseña el controlador (\emph{certainty equivalence}).
  \item \textbf{Directo (implícito):} se reparametriza el problema y se estiman
  directamente los parámetros del controlador.
\end{itemize}

En este trabajo se usa la variante \textbf{indirecta}.

\subsection{Modelo de la planta y ley de control RST}

Se considera un sistema SISO lineal expresado en el operador retardo, con una
perturbación $v_k$ a la entrada:
\begin{equation}
  A(q)\, y_k = B(q)\,(u_k + v_k),
\end{equation}
con $A(q)$ mónico. La ley de control general de dos grados de libertad (RST) es:
\begin{equation}
  \boxed{\,R(q)\, u_k = T(q)\, r_k - S(q)\, y_k\,}
  \label{eq:rst}
\end{equation}
donde $S/R$ es la realimentación (\emph{feedback}) y $T/R$ el prealimentador
(\emph{feedforward}). La salida en lazo cerrado resulta:
\begin{equation}
  y_k = \frac{B\,T}{A R + B S}\, r_k + \frac{B\,R}{A R + B S}\, v_k .
  \label{eq:lc}
\end{equation}

\subsection{Diseño por colocación de polos y ecuación Diofantina}

Se impone el polinomio característico deseado de lazo cerrado $A_{lc}(q)$
mediante la ecuación Diofantina:
\begin{equation}
  A(q)\,R(q) + B(q)\,S(q) = A_{lc}(q).
  \label{eq:diof}
\end{equation}
Esta ecuación tiene solución si $A$ y $B$ no comparten factores. Se resuelve
algebraicamente armando la \textbf{matriz de Sylvester} (concatenación de
matrices de Toeplitz de $A$ y $B$). El \emph{controlador de mínimo orden}
cumple $\deg(R) = \deg(S)$ y $\deg(S) = \deg(A) - 1$, con
$\deg(A_{lc}) = 2\deg(A) - 1$.

\subsection{Rechazo de perturbaciones}

Del numerador de la perturbación en \eqref{eq:lc}, $B\,R$, se desprenden las
reglas de diseño:
\begin{itemize}[nosep]
  \item \textbf{Perturbación de carga escalón en $v$:} $B$ o $R$ deben tener una
  raíz en $z=1$. La clave está en incluir el factor $(1-q^{-1})$ en $R$: como el
  integrador discreto es $1/(1-q^{-1})$, al despejar $u$ aparece su inversa y la
  ley adquiere \emph{acción integral}.
  \item \textbf{Ruido de medición $e$:} para atenuar el efecto de alta frecuencia
  puede incorporarse el factor $z+1$ en $S$. El RLS estándar \textbf{no filtra
  por sí mismo} el ruido de medición y, en un modelo ARX, puede producir
  estimaciones sesgadas (\emph{errores en variables}); esto motiva las mejoras de
  robustez de la Sección~7.
\end{itemize}

\subsection{Estimación recursiva (RLS con olvido)}

Para un modelo ARX, con vector de parámetros
$\theta = [a_1,\dots,a_n, b_1,\dots,b_m]^\top$ y regresor $\varphi(k)$, el RLS
con factor de olvido $\lambda \in (0,1]$ es:
\begin{align}
  \epsilon(k) &= y(k) - \varphi^\top(k)\,\hat\theta(k-1), \\
  K(k) &= \frac{P(k-1)\varphi(k)}{\lambda + \varphi^\top(k)P(k-1)\varphi(k)}, \\
  \hat\theta(k) &= \hat\theta(k-1) + K(k)\,\epsilon(k), \\
  P(k) &= \tfrac{1}{\lambda}\big(I - K(k)\varphi^\top(k)\big)P(k-1).
\end{align}
$\lambda \to 1$ permite seguir parámetros lentamente variables; valores menores
olvidan más rápido a costa de mayor sensibilidad al ruido.

\subsection{Algoritmo indirecto}

\begin{enumerate}[nosep]
  \item En cada $k$, recolectar $\varphi(k)$ e $y(k)$.
  \item Actualizar RLS $\Rightarrow \hat A(q), \hat B(q)$.
  \item Resolver la Diofantina \eqref{eq:diof} $\Rightarrow R, S, T$.
  \item Calcular la acción de control con \eqref{eq:rst}.
  \item Aplicar $u(k)$ y repetir.
\end{enumerate}
No es necesario recalcular el controlador en cada paso: puede hacerse cada $N$
pasos. Es crítico garantizar la \emph{persistencia de excitación}.

% ==================================================================
\section{Sistema: el Péndulo Invertido}
% ==================================================================

\subsection{Modelo no lineal}

La planta ``real'' que se controla en la simulación es el modelo no lineal del
carro-péndulo. Se integran los \textbf{cuatro} estados $[x, \dot x, \theta,
\dot\theta]$ (aunque el control sólo usa $\theta$):
\begin{align}
  \ddot\theta &= \frac{g\sin\theta - \cos\theta \cdot \dfrac{F + m l \dot\theta^2 \sin\theta}{M+m}}
  {l\left(\tfrac{4}{3} - \dfrac{m\cos^2\theta}{M+m}\right)}, \\[0.4em]
  \ddot x &= \frac{F + m l \dot\theta^2 \sin\theta}{M+m} - \frac{m l \ddot\theta \cos\theta}{M+m},
\end{align}
con $M = 1{,}0$~kg (carro), $m = 0{,}1$~kg (barra), $g = 9{,}81$~m/s$^2$ y
$l = 0{,}5$~m la \textbf{distancia del pivote al centro de masa} (media longitud
de la barra); el término $\tfrac{4}{3}$ proviene del momento de inercia de una
barra uniforme. Se integra con Runge-Kutta manteniendo la acción de control
constante en cada período (ZOH). Como no se controla la posición del carro, $x$
y $\dot x$ no intervienen en el diseño; la dinámica angular alcanza para el
control.

\subsection{Linealización}

Se linealiza mediante el \textbf{jacobiano} de $\ddot\theta = f(\theta,
\dot\theta, F)$ evaluado en el equilibrio inestable $(\theta, \dot\theta, F) =
(0, 0, 0)$ (desarrollo de Taylor de primer orden):
\begin{equation}
  A_\theta = \left.\frac{\partial f}{\partial \theta}\right|_{eq}, \qquad
  B_\theta = \left.\frac{\partial f}{\partial F}\right|_{eq}, \qquad
  \left.\frac{\partial f}{\partial \dot\theta}\right|_{eq} = 0,
\end{equation}
donde el término en $\dot\theta$ se anula porque el único aporte, $m l
\dot\theta^2 \sin\theta$, tiene derivada $2 m l \dot\theta \sin\theta = 0$ en el
equilibrio. Evaluando las derivadas (equivalente a la aproximación de ángulo
pequeño $\sin\theta\approx\theta$, $\cos\theta\approx1$ y despreciando el término
cuadrático $\dot\theta^2\sin\theta$) se obtiene:
\begin{equation}
  \ddot\theta = A_\theta\,\theta + B_\theta\,F, \qquad
  A_\theta = \frac{3g(M+m)}{l(4M+m)} = 15{,}79, \qquad
  B_\theta = \frac{-3}{l(4M+m)} = -1{,}4634 .
\end{equation}
El signo $B_\theta < 0$ es físicamente correcto: un empuje $F>0$ sobre el carro
reduce $\theta$. Este signo es esencial: usar el signo opuesto produce
realimentación positiva y el lazo diverge.

\subsection{Discretización ZOH analítica y modelo ARX}

Para $G(s) = B_\theta/(s^2 - w^2)$ con $w = \sqrt{A_\theta}$, el mantenedor de
orden cero da:
\begin{equation}
  A(z) = z^2 - 2\cosh(w T_s)\, z + 1, \qquad
  B(z) = \frac{B_\theta(\cosh(w T_s) - 1)}{A_\theta}\,(z + 1),
\end{equation}
con $T_s = 0{,}02$~s. Numéricamente, el modelo ARX nominal es:
\begin{equation}
  A(q) = 1 - 2{,}0063\,q^{-1} + 1{,}0\,q^{-2}, \qquad
  B(q) = -2{,}93\!\times\!10^{-4}\,(q^{-1} + q^{-2}).
\end{equation}

\paragraph{Observaciones de diseño.} La planta discreta tiene un polo en
$z \approx 1{,}083$ (\emph{fuera} del círculo unitario, inestable) y otro en
$z \approx 0{,}924$. El cero está en $z \approx -1$ (sobre el círculo unitario):
es \textbf{no de fase mínima}, por lo que \textbf{no se lo cancela} (cancelarlo
generaría oscilaciones de alta amplitud, \emph{ringing}). Este modelo nominal se
usa solo como \emph{prior} de arranque del STR; los parámetros reales se estiman
on-line.

% ==================================================================
\section{Diseño del Regulador STR (paso a paso)}
% ==================================================================

\subsection{Estructura elegida}

STR indirecto, por colocación de polos, \textbf{sin cancelación de ceros} y con
\textbf{acción integral}. El factor $1-q^{-1}$ es el operador \emph{diferencia};
el integrador discreto es $1/(1-q^{-1})$. Al incluir $1-q^{-1}$ en $R$, al
despejar $u$ de \eqref{eq:rst} aparece su inversa y la ley de control adquiere
acción integral. Para ello se diseña sobre
\begin{equation}
  \bar A(q) = A(q)\,(1 - q^{-1}),
\end{equation}
de grado 3, y luego $R = (1-q^{-1})\,\bar R$.

\subsection{Elección de los polos de lazo cerrado}

Se reutiliza la especificación del diseño previo ($s = -3, -3, -10$),
mapeada a discreto con $z = e^{s T_s}$:
\begin{equation}
  \{-3,-3,-10\} \;\to\; \{0{,}9418,\ 0{,}9418,\ 0{,}8187\}.
\end{equation}
La Diofantina con acción integral requiere $\deg(A_{lc}) = 5$, por lo que se
agregan dos polos auxiliares rápidos en $z = 0{,}2$ (bien amortiguados, cerca
del origen) para completar el grado sin alterar la dinámica dominante. Para
mantener coherencia con la ecuación Diofantina (planteada en $q^{-1}$), se
escribe:
\begin{equation}
  A_{lc}(q^{-1}) = \prod_{i=1}^{5}\big(1 - p_i\,q^{-1}\big),
  \qquad p_i \in \{0{,}9418,\ 0{,}9418,\ 0{,}8187,\ 0{,}2,\ 0{,}2\}.
\end{equation}
Sus coeficientes son los que devuelve \texttt{np.poly(polos\_lc)}.

\subsection{Grados y causalidad}

Con $\deg(\bar A) = 3$ y $\deg(B) = 2$, el controlador de mínimo orden tiene
$\deg(\bar R) = 2$ y $\deg(S) = 2$. El regulador final:
\begin{equation}
  R(q) = (1-q^{-1})\,\bar R(q) \ (\text{grado }3), \qquad
  S(q) = s_0 + s_1 q^{-1} + s_2 q^{-2}.
\end{equation}

\subsection{Resolución de la ecuación Diofantina (matriz de Sylvester)}

Se resuelve $\bar A(q)\,\bar R(q) + B(q)\,S(q) = A_{lc}(q)$ con
$\bar A = [1, \bar a_1, \bar a_2, \bar a_3]$, $B = [0, b_1, b_2]$,
$\bar R = [1, r_1, r_2]$ y $S = [s_0, s_1, s_2]$. Igualando coeficientes de
$q^{-1}$ a $q^{-5}$ se obtiene el sistema lineal (matriz de Sylvester):
\begin{equation}
\begin{bmatrix}
1 & 0 & b_1 & 0 & 0 \\
\bar a_1 & 1 & b_2 & b_1 & 0 \\
\bar a_2 & \bar a_1 & 0 & b_2 & b_1 \\
\bar a_3 & \bar a_2 & 0 & 0 & b_2 \\
0 & \bar a_3 & 0 & 0 & 0
\end{bmatrix}
\begin{bmatrix} r_1 \\ r_2 \\ s_0 \\ s_1 \\ s_2 \end{bmatrix}
=
\begin{bmatrix}
\alpha_1 - \bar a_1 \\ \alpha_2 - \bar a_2 \\ \alpha_3 - \bar a_3 \\ \alpha_4 \\ \alpha_5
\end{bmatrix},
\end{equation}
donde $\alpha_i$ son los coeficientes de $A_{lc}$. Para el modelo nominal
($\bar A = [1, -3{,}0063,\ 3{,}0063,\ -1{,}0]$) la solución es:
\begin{equation}
  R = [\,1,\ -1{,}2033,\ 0{,}2324,\ -0{,}0290\,], \quad
  S = [\,-366{,}77,\ 697{,}19,\ -331{,}09\,].
\end{equation}
Los valores grandes de $S$ reflejan la alta ganancia necesaria para estabilizar
un sistema inestable.

\subsection{Prealimentación (ganancia estática unitaria)}

Se impone $y/r|_{q^{-1}=1} = 1$, es decir $B(1)\,T / A_{lc}(1) = 1$, de donde
\begin{equation}
  T = t_0 = \frac{A_{lc}(1)}{B(1)} = \frac{\sum_i \alpha_i}{b_1 + b_2} = -0{,}6718 .
\end{equation}
Como la referencia es $\theta = 0$, el término $T\,r$ es nulo, pero se deja
general.

\subsection{Diagrama de bloques}

\begin{figure}[H]
\centering
\begin{tikzpicture}[>=Latex, node distance=1.4cm, auto,
  block/.style={draw, rectangle, minimum height=1cm, minimum width=1.6cm, align=center},
  sum/.style={draw, circle, inner sep=0pt, minimum size=6mm}]
  \node (r) {$r_k$};
  \node[block, right=1.2cm of r] (T) {$\dfrac{T}{R}$};
  \node[sum, right=1.2cm of T] (s1) {$+$};
  \node[block, right=1.2cm of s1] (P) {$\dfrac{B}{A}$ \\ \small planta};
  \node[right=1.6cm of P] (y) {$y_k$};
  \node[block, below=1.2cm of s1] (S) {$\dfrac{S}{R}$};

  \draw[->] (r) -- (T);
  \draw[->] (T) -- (s1);
  \draw[->] (s1) -- node {$u_k$} (P);
  \draw[->] (P) -- (y);
  \draw[->] ($(P)!0.5!(y)$) |- (S);
  \draw[->] (S) -| node[pos=0.95, left] {$-$} (s1);
\end{tikzpicture}
\caption{Estructura RST de dos grados de libertad: feedforward $T/R$,
feedback $S/R$ y planta $B/A$. La ley $R u = T r - S y$ incluye el factor
$(1-q^{-1})$ en $R$, que introduce acción integral.}
\label{fig:blk}
\end{figure}

% ==================================================================
\section{Implementación}
% ==================================================================

El código está en \texttt{respuesta\_pid\_pendulo\_autotunning.py}. Se resumen
las piezas centrales.

\subsection{Estimador RLS}

\begin{lstlisting}[caption={Un paso de RLS con olvido.}]
def rls_step(theta, P, phi, y_meas, lambda_=0.995):
    y_pred = phi @ theta
    err = y_meas - y_pred
    Pphi = P @ phi
    K = Pphi / (lambda_ + phi @ Pphi)
    theta = theta + K * err
    P = (P - np.outer(K, Pphi)) / lambda_
    return theta, P, err
\end{lstlisting}

\subsection{Ecuacion Diofantina (Sylvester) y diseno RST}

\begin{lstlisting}[caption={Armado de la matriz de Sylvester y resolucion.}]
def solve_diophantine_sylvester(A_bar, B, Alc, deg_R, deg_S):
    n_rows = len(Alc)
    n_unk = deg_R + (deg_S + 1)
    M_syl = np.zeros((n_rows, n_unk))
    for i in range(1, deg_R + 1):          # columnas de r_i (A_bar desplazado)
        for k in range(len(A_bar)):
            M_syl[i + k, i - 1] += A_bar[k]
    off = deg_R
    for j in range(0, deg_S + 1):          # columnas de s_j (B desplazado)
        for k in range(len(B)):
            M_syl[j + k, off + j] += B[k]
    rhs = Alc.copy()
    for k in range(len(A_bar)):            # pasar termino monico de R a la der.
        rhs[k] -= A_bar[k]
    sol = np.linalg.solve(M_syl[1:1 + n_unk, :], rhs[1:1 + n_unk])
    return np.concatenate(([1.0], sol[:deg_R])), sol[deg_R:]
\end{lstlisting}

\begin{lstlisting}[caption={Diseno del regulador con accion integral y sin cancelacion.}]
def design_rst(theta_hat, polos_lc=POLOS_LC):
    a1, a2, b1, b2 = theta_hat
    A = np.array([1.0, a1, a2])
    B = np.array([0.0, b1, b2])
    A_bar = np.convolve(A, [1.0, -1.0])    # accion integral: A*(1 - q^-1)
    Alc = np.real(np.poly(polos_lc))       # A_lc deseado
    R_bar, S = solve_diophantine_sylvester(A_bar, B, Alc, deg_R=2, deg_S=2)
    R = np.convolve([1.0, -1.0], R_bar)
    t0 = np.sum(Alc) / (b1 + b2)           # ganancia estatica unitaria
    return R, S, t0
\end{lstlisting}

\subsection{Controlador RST}

\begin{lstlisting}[caption={Ley recursiva R u = T r - S y.}]
def control(self, r, y):
    yvec = np.concatenate(([y], self.y_hist))[:len(self.S)]
    s_term = float(self.S @ yvec)
    r_term = float(self.R[1:] @ self.u_hist[:len(self.R) - 1])
    u = (self.t0 * r - s_term - r_term) / self.R[0]
    # ... actualizar historiales de u e y ...
    return u
\end{lstlisting}

\subsection{Bucle adaptativo}

En cada paso: (i) RLS actualiza $\hat\theta = [a_1,a_2,b_1,b_2]$; (ii) tras un
\emph{warmup} y cada \texttt{redesign\_every} pasos se rediseña el RST; (iii) se
aplica la acción de control; (iv) se integra la ODE no lineal (ZOH). Dos
salvaguardas importantes:
\begin{itemize}[nosep]
  \item \textbf{Freno por baja excitación:} el RLS solo actualiza si
  $\lVert\varphi\rVert$ supera un umbral. Al estabilizarse el péndulo, las
  señales tienden a cero y sin este freno el estimador se degradaría (problema
  de persistencia de excitación).
  \item \textbf{Guarda de divergencia:} si $|\theta| > 90^\circ$ (el péndulo
  ``cayó'') se detiene la integración.
\end{itemize}

\begin{lstlisting}[caption={Nucleo del bucle de simulacion.}]
if k >= 2:
    phi = np.array([-y[k-1], -y[k-2], u[k-1], u[k-2]])
    if np.linalg.norm(phi) > excite_thresh:
        theta_hat, P, _ = rls_step(theta_hat, P, phi, y_measured, lambda_)

if k >= warmup_k and (k - warmup_k) % redesign_every == 0:
    R_new, S_new, t0_new = design_rst(theta_hat)   # certainty equivalence
    ctrl_rst.set_params(R_new, S_new, t0_new)

u_control = ctrl_rst.control(ref[k], y_measured)
\end{lstlisting}

% ==================================================================
\section{Resultados y Validación}
% ==================================================================

Se simularon 20~s desde la condición inicial $\theta(0) = 0{,}1$~rad con
referencia $\theta = 0$, en cuatro escenarios. En todos, el ángulo se estabiliza
(nunca supera la condición inicial) y la acción de control se mantiene acotada
($|u|_{\max} \approx 37$~N). Sin perturbaciones, la estimación RLS converge al
modelo real
$[a_1,a_2,b_1,b_2] \approx [-2{,}006,\ 1{,}0,\ -2{,}9\!\times\!10^{-4},\ -2{,}9\!\times\!10^{-4}]$.
El análisis de convergencia del regulador ante ruido y perturbaciones se detalla
en la Sección~7.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{presentacion_pid_pendulo/imagenes_pid_autotunning/str_polos_ceros.png}
\caption{Mapa de polos y ceros discreto. El polo de lazo abierto fuera del
círculo unitario (planta inestable) y el cero en $z\approx-1$; los polos de lazo
cerrado impuestos por el STR quedan dentro del círculo unitario.}
\label{fig:pz}
\end{figure}

\begin{figure}[H]
\centering
\begin{subfigure}{0.49\textwidth}
  \includegraphics[width=\textwidth]{presentacion_pid_pendulo/imagenes_pid_autotunning/str_Sin_Perturbaciones.png}
  \caption{Sin perturbaciones}
\end{subfigure}
\hfill
\begin{subfigure}{0.49\textwidth}
  \includegraphics[width=\textwidth]{presentacion_pid_pendulo/imagenes_pid_autotunning/str_Pert_Sinusoidal.png}
  \caption{Perturbación sinusoidal (en la salida)}
\end{subfigure}
\\[0.5em]
\begin{subfigure}{0.49\textwidth}
  \includegraphics[width=\textwidth]{presentacion_pid_pendulo/imagenes_pid_autotunning/str_Pert_Escalon.png}
  \caption{Perturbación rectangular (pulso $t\in[6,8]$, en la salida)}
\end{subfigure}
\hfill
\begin{subfigure}{0.49\textwidth}
  \includegraphics[width=\textwidth]{presentacion_pid_pendulo/imagenes_pid_autotunning/str_Ruido_Medicion.png}
  \caption{Ruido de medición ($\sigma=0{,}005$, sólo en $y_{\text{medida}}$)}
\end{subfigure}
\caption{Respuesta ($\theta$), acción de control ($u$), estimación RLS de
$A(q),B(q)$ y coeficientes del regulador RST para los cuatro escenarios.}
\label{fig:res}
\end{figure}

\paragraph{Puntos de inyección.} Las tres perturbaciones tienen transferencias
distintas y se aplican en lugares distintos: la \emph{sinusoidal} y el
\emph{pulso rectangular} se suman a la salida verdadera $\theta$ (perturbación de
\emph{salida}, no de carga), mientras que el \emph{ruido} se agrega
exclusivamente a la salida medida $y_{\text{medida}}$ (ruido de medición).

El máximo de $|\theta|$ está dominado por la condición inicial ($0{,}1$~rad) y
por eso aporta poca información. La Tabla~\ref{tab:met} usa métricas más
representativas del efecto de las perturbaciones: el RMS y el máximo de $\theta$
\emph{después} del transitorio ($t>5$~s), el tiempo de establecimiento
($|\theta|<0{,}02$), el esfuerzo de control (RMS de $u$) y la dispersión de
$s_0$ en la ventana final (indicador de convergencia del regulador). Se reportan
para el modo robusto (el análisis básico vs.\ robusto está en la
Sección~\ref{sec:robustez}).

\begin{table}[H]
\centering
\caption{Métricas de desempeño (modo robusto).}
\label{tab:met}
\begin{tabular}{@{}lccccc@{}}
\toprule
Escenario & RMS$_\theta^{t>5}$ & $|\theta|_{\max}^{t>5}$ & $t_{\text{est}}$ [s]
 & RMS$_u$ & std$(s_0)$ \\
\midrule
Sin perturbaciones      & $0{,}0000$ & $0{,}0000$ & $0{,}36$ & $1{,}27$ & $\approx 0$ \\
Perturbación sinusoidal & $0{,}0134$ & $0{,}0189$ & $0{,}08$ & $1{,}38$ & $\approx 0$ \\
Pulso rectangular       & $0{,}0062$ & $0{,}0344$ & $8{,}24$ & $1{,}33$ & $\approx 0$ \\
Ruido de medición       & $0{,}0026$ & $0{,}0072$ & $0{,}36$ & $2{,}75$ & $\approx 0$ \\
\bottomrule
\end{tabular}
\end{table}

El factor $(1-q^{-1})$ en $R$ dota al lazo de \emph{acción integral}, lo que
permite rechazar perturbaciones de tipo escalón. La perturbación de la
Figura~\ref{fig:res}(c) es en rigor un \emph{pulso rectangular}
(escalón de subida en $t=6$~s y de bajada en $t=8$~s): el $t_{\text{est}}$
de $8{,}24$~s refleja el rechazo del flanco de subida y la recuperación tras el
de bajada. Como el pulso desaparece antes de alcanzar necesariamente el régimen
permanente, no se lo usa para afirmar ``error estacionario nulo''; esa propiedad
se sigue de la acción integral ante una perturbación de escalón sostenida.

% ==================================================================
\section{Análisis de estabilidad y convergencia; robustez}
\label{sec:robustez}
% ==================================================================

El ángulo $\theta$ permanece acotado en los cuatro escenarios, pero un análisis
más fino revela que \textbf{los coeficientes del regulador $R,S,T$ no convergen}
con perturbación sinusoidal ni con ruido de medición. Esta sección lo diagnostica,
lo corrige con una variante \emph{robusta} y valida todo con una suite de tests.

\subsection{Verificación del diseño}

Con el modelo nominal, la solución de la Diofantina reproduce \emph{exactamente}
los polos deseados: las raíces de $A(q)R(q)+B(q)S(q)$ coinciden con $A_{lc}$
($\max|z| = 0{,}9418 < 1$). Los polos elegidos (mapeo $e^{sT_s}$ de $-3,-3,-10$
más dos auxiliares en $0{,}2$) son sensatos: reales, positivos y bien dentro del
círculo unitario. El test \texttt{test\_diophantine\_nominal} verifica esta
igualdad automáticamente.

\subsection{Por qué el regulador no converge (modo básico)}

La ley de control aplicada a la planta \emph{real} usa el regulador diseñado con
el modelo \emph{estimado}. Cuando ese estimado se degrada, el regulador
``persigue'' el ruido. La causa raíz es que el numerador $B$ es diminuto
($b_1{+}b_2 \approx -5{,}9\times10^{-4}$) y aparece en el \emph{denominador} del
diseño ($t_0 = A_{lc}(1)/B(1)$ y la matriz de Sylvester): un error relativo en
$b$ se amplifica enormemente en $R,S,T$. Concretamente:
\begin{itemize}[nosep]
  \item \textbf{Perturbación sinusoidal (en la salida).} Al sumarse a $\theta$,
  la sinusoide contamina el regresor del RLS, que reparte mal la ganancia:
  $b_1$ llega a cambiar de signo, el cero estimado salta a $z\approx+1{,}7$ y los
  coeficientes de $S$ crecen \emph{monótonamente} ($s_1$ pasa de $\sim700$ a
  $\sim3000$, Fig.~\ref{fig:cmpdet_sin}). El radio de polos de LC (evaluado sobre
  el modelo nominal) trepa de $0{,}942$ a $0{,}989$: el lazo sigue estable pero
  pierde margen de forma sostenida.
  \item \textbf{Ruido de medición (sólo en $y_{\text{medida}}$).} El RLS adapta
  sobre el ruido, agravado por \emph{covariance windup} ($\lambda<1$ con baja
  excitación): $a_1,a_2$ quedan sesgados, $s_0$ fluctúa $\pm20\%$ y, en algunos
  rediseños, el regulador instantáneo llega a colocar polos \emph{fuera} del
  círculo unitario ($\max|z|>1$, Fig.~\ref{fig:cmpdet_rui}). El lazo sobrevive
  por ser lentamente variable, pero el diseño es momentáneamente inestable.
\end{itemize}

\subsection{Mejoras de robustez}

Se agrega una variante robusta (\texttt{robust=True}), comparable con la básica.
Cada mecanismo ataca una de las causas anteriores:
\begin{itemize}[nosep]
  \item \textbf{Zona muerta} en el RLS (contra el ruido y la perturbación de
  salida): no se actualiza si $|\varepsilon|$ no supera el error de ecuación
  esperado ($\sim\sigma\lVert[1,a_1,a_2]\rVert$), evitando que el estimador
  ``persiga'' señales que no son la dinámica de la planta.
  \item \textbf{Anti \emph{windup} de covarianza} (contra el \emph{covariance
  windup} del caso de ruido): se acota $\mathrm{tr}(P)$ para que la ganancia del
  RLS no crezca sin límite bajo baja excitación.
  \item \textbf{Gate de rediseño} (contra los diseños con $b$ mal estimado):
  validado contra el \emph{prior nominal confiable} (no contra la estimación
  ruidosa), acepta un candidato sólo si $b_1{+}b_2$ conserva signo y magnitud,
  $|t_0|$ y $\lVert S\rVert$ son razonables y el lazo cerrado resultante es
  estable. Además sólo se rediseña ante cambios significativos de $\hat\theta$
  (congelamiento). \emph{Limitación:} el gate sólo garantiza estabilidad respecto
  del \emph{prior nominal}; no puede asegurarla frente a una planta real
  desconocida.
\end{itemize}

\begin{lstlisting}[caption={Gate de aceptación del regulador candidato (robusto).}]
def candidato_valido(R, S, t0, theta_hat, s_norm_max, cfg, plant_ref):
    b1, b2 = theta_hat[2], theta_hat[3]
    if (b1 + b2) >= 0 or abs(b1 + b2) < cfg["b_min"]:   # signo/magnitud de b
        return False
    if abs(t0) > cfg["t0_max"] or np.linalg.norm(S) > s_norm_max:
        return False
    poles = closed_loop_poles(plant_ref, R, S)          # estabilidad vs prior
    return np.max(np.abs(poles)) < 1.0 - cfg["pole_margin"]
\end{lstlisting}

\subsection{Resultados comparativos}

La variante robusta hace converger $R,S,T$ y recupera exactamente los polos de
diseño ($\max|z| = 0{,}9418$) en los cuatro escenarios, mientras la básica deriva
(Tabla~\ref{tab:conv}, Figuras~\ref{fig:conv}, \ref{fig:cmpdet_sin} y
\ref{fig:cmpdet_rui}).

\begin{table}[H]
\centering
\caption{Máximo módulo de los polos de lazo cerrado \emph{evaluados sobre el
modelo lineal nominal alrededor de $\theta=0$} (con el regulador final) y
convergencia (desvío de $s_0$ en la ventana final). El gate del modo robusto
sólo verifica estabilidad frente a este modelo nominal, no frente a la planta
real desconocida.}
\label{tab:conv}
\begin{tabular}{@{}lcccc@{}}
\toprule
& \multicolumn{2}{c}{Básico} & \multicolumn{2}{c}{Robusto} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Escenario & $\max|z|$ & std$(s_0)$ & $\max|z|$ & std$(s_0)$ \\
\midrule
Sin perturbaciones      & 0{,}942 & $\approx 0$ & 0{,}942 & $0$ \\
Perturbación sinusoidal & 0{,}989 & $190$       & 0{,}942 & $0$ \\
Pulso rectangular       & 0{,}957 & $\approx 0$ & 0{,}942 & $0$ \\
Ruido de medición       & 0{,}995 & $126$       & 0{,}942 & $0$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{presentacion_pid_pendulo/imagenes_pid_autotunning/str_convergencia.png}
\caption{Desvío estándar de $s_0$ en la ventana final (escala log). El modo
robusto (azul, prácticamente nulo) converge en todos los escenarios; el básico
(rojo) no converge con perturbación sinusoidal ni con ruido.}
\label{fig:conv}
\end{figure}

\paragraph{Sinusoidal (los dos casos que más cambian).} La
Figura~\ref{fig:cmpdet_sin} detalla el caso sinusoidal. Es importante ser
honestos: en régimen, el modo básico logra un RMS de $\theta$ \emph{menor}
($0{,}0040$ vs $0{,}0134$), porque su ganancia $s_1$ crece sin límite y eso
mejora el rechazo de la perturbación de salida a $0{,}5$~Hz. Sin embargo, ese
mismo crecimiento es lo que aleja los polos hacia el círculo unitario
($0{,}942\!\to\!0{,}989$) y hace que $R,S,T$ nunca converjan. El robusto
sacrifica algo de rechazo del rizo a cambio de coeficientes convergidos y margen
de estabilidad constante.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{presentacion_pid_pendulo/imagenes_pid_autotunning/str_cmp_detalle_Pert_Sinusoidal.png}
\caption{Perturbación sinusoidal: básico vs robusto. Arriba: $\theta$ en régimen
(con RMS) y control $u$. Abajo: coeficientes de $S$ (básico en trazo punteado,
diverge; robusto sólido, constante) y radio de polos de LC (el básico sube a
$0{,}989$; el robusto se mantiene en $0{,}942$).}
\label{fig:cmpdet_sin}
\end{figure}

\paragraph{Ruido de medición.} La Figura~\ref{fig:cmpdet_rui} detalla el caso de
ruido, donde el robusto es netamente superior: el RMS de $\theta$ en régimen baja
de $0{,}0165$ a $0{,}0026$ (unas $6\times$), el tiempo de establecimiento pasa de
$\sim20$~s a $0{,}36$~s y el radio de polos deja de superar el círculo unitario.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{presentacion_pid_pendulo/imagenes_pid_autotunning/str_cmp_detalle_Ruido_Medicion.png}
\caption{Ruido de medición: básico vs robusto. El regulador básico persigue el
ruido ($S$ oscila y el radio de polos supera $|z|=1$ en varios instantes); el
robusto se estabiliza en el diseño nominal y regula mucho mejor a $\theta$.}
\label{fig:cmpdet_rui}
\end{figure}

\subsection{Demostración con planta distinta a priori}
\label{sec:selftuning}

En los cuatro escenarios anteriores el prior nominal coincide con la
linealización de la planta, de modo que la variante robusta recupera y
congela el regulador nominal. Eso muestra robustez del controlador
nominal, pero no prueba por sí solo la capacidad de \emph{autoajuste}: para ello
hace falta que la planta real difiera del prior.

\paragraph{Caso 1: Carrito $50\%$ más pesado (cambia la ganancia).} Con
$M_{\text{real}} = 1{,}5\,M_{\text{nominal}}$ y prior nominal, la ganancia real
cambia de $b_1{+}b_2 \approx -5{,}9\times10^{-4}$ a $\approx -3{,}9\times10^{-4}$.
Como sin excitación la tarea de regulación pierde persistencia, se inyecta una
referencia cuadrada de pequeña amplitud (dither) y se reduce la zona muerta. Los
resultados (Fig.~\ref{fig:selftuning}) confirman el autoajuste:
\begin{enumerate}[nosep]
  \item el RLS arranca del prior nominal \emph{incorrecto};
  \item la ganancia estimada se desplaza hacia el modelo real
  ($b_1{+}b_2:\ -5{,}9 \to -3{,}9\times10^{-4}$);
  \item el RST se rediseña: $S$ pasa de $[-367,697,-331]$ (nominal) a
  $\approx[-551,1048,-498]$, que coincide con el diseño calculado para la planta
  real ($[-545,1037,-492]$);
  \item el controlador final \textbf{difiere} del nominal y el lazo permanece
  estable ($\max|z|<1$ sobre la planta real, $|\theta|_{\max}=0{,}10$).
\end{enumerate}
Es notable que en este caso $a_1,a_2$ \emph{no} se mueven. La razón es física:
$a_2 \equiv 1$ es estructural (el producto de los polos discretos $e^{\pm wT_s}$
del péndulo sin fricción es $1$), y $a_1 = -2\cosh(wT_s)$ depende de
$A_\theta = 3g(M+m)/(l(4M+m))$, que es casi \textbf{insensible a la masa del
carro} (numerador y denominador crecen ambos con $M$ y el cociente satura en
$3g/4l$). La masa afecta esencialmente sólo la ganancia $B_\theta \propto 1/M$, es
decir $b$. El RLS mueve exactamente lo que cambió.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{presentacion_pid_pendulo/imagenes_pid_autotunning/str_self_tuning.png}
\caption{Autoajuste ante un carro $50\%$ más pesado. Estimados (línea continua)
parten del prior nominal (punteado) y van hacia la planta real (guiones): $b$
converge al real; $a_1$ (eje ampliado) casi no cambia porque la masa apenas
afecta los polos. El regulador $S$ se rediseña hacia el diseño de la planta real.}
\label{fig:selftuning}
\end{figure}

\paragraph{Caso 2: barra $10\%$ más larga (cambian los polos y la ganancia).}
Para mostrar además el desplazamiento de los \emph{polos}, se alarga la barra un
$10\%$ ($l_{\text{real}} = 1{,}1\,l$), lo que sí cambia $A_\theta$ y por lo tanto
$a_1$ (de $-2{,}00632$ a $-2{,}00575$). Como $a_1$ multiplica a $y\approx 0$
(péndulo regulado), está mucho peor excitado que $b$ (que multiplica a $u$,
grande): para identificarlo hace falta \textbf{más excitación} (dither de mayor
amplitud) y \textbf{menor zona muerta}. Con esos ajustes (Fig.~\ref{fig:selftuning_l})
el $a_1$ estimado se desplaza claramente desde el nominal hacia el real
($-2{,}00632 \to -2{,}00599$), $b$ converge al real y $S$ se rediseña a
$\approx[-402,766,-364]$ (diseño de la planta real), con el lazo estable.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{presentacion_pid_pendulo/imagenes_pid_autotunning/str_self_tuning_l.png}
\caption{Autoajuste ante una barra $10\%$ más larga. A diferencia del caso de
masa, ahora \textbf{$a_1$ sí se mueve} (eje ampliado) desde el prior nominal
hacia la planta real, junto con $b$; el regulador $S$ converge al diseño de la
planta real.}
\label{fig:selftuning_l}
\end{figure}

\subsection{Suite de validación}

El archivo \texttt{test\_str\_pendulo.py} automatiza la validación (todos los
tests pasan):
\begin{itemize}[nosep]
  \item \texttt{test\_diophantine\_nominal}: el diseño nominal reproduce $A_{lc}$.
  \item \texttt{test\_estabilidad\_lazo}: $\max|z|<1$ en los 4 escenarios y ambos
  modos.
  \item \texttt{test\_theta\_acotado}: $|\theta|\le 0{,}12$ en todos los casos.
  \item \texttt{test\_convergencia\_RST\_robusto} y
  \texttt{test\_robusto\_mejora\_convergencia}: $R,S,T$ convergen en robusto y
  mejoran respecto del básico en sinusoidal/ruido.
  \item \texttt{test\_convergencia\_RLS\_robusto}: $\hat\theta$ cercano al modelo
  real cuando el prior coincide con la planta.
  \item \texttt{test\_autoajuste\_planta\_distinta}: con la planta $50\%$ más
  pesada, la ganancia estimada converge al modelo \emph{real} (no al prior), el
  $S$ final difiere del nominal y el lazo es estable sobre la planta real.
  \item \texttt{test\_autoajuste\_longitud\_mueve\_a1}: con la barra $10\%$ más
  larga, el $a_1$ estimado se desplaza desde el nominal hacia el real y el lazo
  permanece estable.
\end{itemize}

% ==================================================================
\section{Discusión}
% ==================================================================

\begin{itemize}[nosep]
  \item \textbf{Persistencia de excitación.} Al ser una tarea de regulación
  ($\theta \to 0$), una vez estabilizado el sistema la excitación se desvanece.
  Por eso el RLS arranca desde el \emph{prior} nominal y se congela la
  actualización con baja excitación: así el \emph{certainty equivalence} se
  mantiene bien condicionado y estable. Iniciar el estimador desde un prior
  neutro provocó, en pruebas, deriva de parámetros y divergencia.
  \item \textbf{No cancelación de ceros.} El cero en $z\approx-1$ es no de fase
  mínima; cancelarlo produciría \emph{ringing}. La colocación de polos sin
  cancelación evita este problema.
  \item \textbf{Comparación con el enfoque previo.} El STR discreto obtiene el
  regulador directamente en $z$ resolviendo la Diofantina, eliminando la
  conversión a continuo por Tustin y la colocación de polos en $s$. El
  resultado es equivalente pero conceptualmente más directo y consistente con la
  implementación digital.
\end{itemize}

% ==================================================================
\section{Conclusiones}
% ==================================================================

Se implementó un STR indirecto en tiempo discreto que estabiliza la dinámica
angular del péndulo invertido. El método combina estimación RLS on-line,
resolución de la ecuación Diofantina por matriz de Sylvester (con acción integral
y sin cancelar el cero no de fase mínima) y la ley de control RST. El diseño
nominal reproduce exactamente los polos deseados y la variante robusta mantiene
el desempeño acotado ante perturbaciones y ruido de medición, con mejoras claras
en el caso de ruido (RMS de $\theta$ unas $6\times$ menor y margen de estabilidad
preservado).

Debido a la pérdida de persistencia de excitación en la tarea de regulación y al
uso de un prior nominal próximo a la planta, los cuatro escenarios principales
demuestran sobre todo \textbf{estabilidad y robustez}. La \emph{capacidad de
autoajuste} propiamente dicha se validó por separado (Sección~\ref{sec:selftuning}):
con una planta $50\%$ más pesada y excitación persistente, el RLS se desplaza
desde el prior nominal hacia el modelo real, el RST se rediseña a un controlador
\emph{distinto} del nominal y el lazo permanece estable, cumpliendo la definición
de \emph{self-tuning}. En un escenario con incertidumbre paramétrica mayor, el
autoajuste requeriría relajar el gate de rediseño (que hoy sólo garantiza
estabilidad frente al prior) e introducir excitación explícita.

Como material adicional se generan animaciones (\texttt{.mp4}) de cada escenario
en \texttt{saved\_runs\_pid\_autotunning/}.

\end{document}
